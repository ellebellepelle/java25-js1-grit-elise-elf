45608c70e9d427901d32021a61834513
Nyckel till API

Ett API är som en tjänst på internet som skickar data till mig.
Jag frågar OpenWeather om vädret, OpenWeather skickar tillbaka information

fetch() är sättet i JavaScript att hämta något från internet.

Geocoding betyder att jag omvandlar ett stadsnamn till koordinater:
* latitud och * longitud
latitud och longitud är ett sätt att peka ut en plats på jorden.
* användaren skriver: Paris
* geocoding API svarar med lat och lon 
* jag använder dem för att hämta vädret.

STEG!!
1. Bygg en väldigt enkel HTML-sida
2. Koppla script.js till HTML-filen
3. Kontrollera att JavaScript fungerar
4. Skapa ett inputfält och en knapp
5. Få JS att läsa det användaren skriver
6. Hämta latitud och longitud från geocoding API
7. Hämta väderdata från weather API
*** Geocoding API och weather API är två olika tjänster från OpenWeather.
*** Min app behöver först förstå vilken plats användaren menar, och sedan hämta
*** vädret för den platsen. Geocoding API används för att ta ett platsnamn och 
*** göra om det till koordinater (longitud och latitud) Jag skriver in Stockholm
*** och geocoding API svarar namn: stockholm, lat: 59.3293, lon: 18.0686
*** Alltså: Geocoding API = från stadsnamn -> koordinater 
*** Geocoding API är en tolk mellan min kod (stadsnamnet) och koordinaten (long, lat)
*** Weather API används för att hämta själva väderinformationen.
*** jag har long och lat, jag skickar dem till weather API och får tillbaka:
*** ex. *väderbeskrivning, *temperatur, *vindhastighet, *ikonkod
*** Alltså: Weather API = från koordinater -> till väderdata
*** ** Vilka koordinater har den här staden? -> Geocoding API
*** ** Vad är värdet på de koordinaterna? -> Weather API
8. Visa vädret på sidan
9. Lägg till felmeddelanden
10. Testa allt noggrant
11. När allt fungerar, gör css.




1. Bygg en enkel HTML. OK
2. Kontrollera att js funkar. OK
3. Få knappen att funka. OK
4. hämta inputfältet via id och få det att funka. OK
5. Kontrollera om input är tom. OK
6. API-anrop, hämta lon och lat med geocoding API, prata med OpenWeather.
Ett API-anrop är i princip en webbadress. Jag bygger en adress som innehåller:
* stadens namn och * min API-Nyckel
Sedan använder jag fetch() för att hämta svaret.
*** VIKTIGT *** jag behöver lägga in min API-nyckel i JS: const apiKey = "45608c70e9d427901d32021a61834513";
Testa hämta koordinaterna, första riktiga API-testet: geocoding. OK
7. Fick tillbaka en array med data, namn, lat, lon, country. i JSON
OpenWeather skickade tillbaka en lista med träffar, eftersom jag satte limit=1 så får jag bara en träffa
Det betyder att jag oftast jobbar med första platsen i listan: data[0] dvs, första objektet i listan.
Det objektet innehåller data med namn på olika delar:
namn är ett fält, lat är ett fält, lon är ett fält.
*** Om jag vill läsa latituden så skriver jag : data[0].lat
8. Plocka ut lat och lon / läsa ut koordinaterna.
if (data.length === 0) -> listan är tom/ingen stad hittades
const lat = data[0].lat; -> sparar latituden
const lon = data[0].lon; -> sparar longituden
9. Nu när jag har lat och lon kan jag hämta vädret.
Jag gör ett andra API-anrop till weather-API 
10. Provar appen igen, skriver en stad och sök, ska få ut ett stort objekt med väderdata.
Ska plocka ut :
Beskrivning - väderData.weather[0].description
Temperatur - väderData.main.temp
Vindhastighet - väderData.wind.speed
Ikonkod - väderData.weather[0].icon
Nu, skriv ut resultatet i HTML
